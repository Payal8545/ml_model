# comment-toxicity

Local toxic-comment tools: **`my-toxic-detector`** (BERT + FastAPI + Chrome extension) and **`my-toxic-detector-no-bert`** (HateSonar + profanity, port 8001). Load the **`extension`** subfolder in Chrome as an unpacked extension; run `server.py` from the matching project folder.
